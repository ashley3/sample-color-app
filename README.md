# sample-color-app

This is a basic Android app written in Kotlin that saves user input colors to a local database. During class we decided to extend the project by adding click handling which opens a new activity for the clicked on color.
