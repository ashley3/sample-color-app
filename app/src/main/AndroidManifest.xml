<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.colors" >

    <application
        android:name="com.example.colors.ColorApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Colors" >
        <activity
            android:name="com.example.colors.ui.ColorDetailsActivity"
            android:exported="false"
            android:label="@string/title_activity_color_details"
            android:theme="@style/Theme.Colors.NoActionBar" />
        <activity
            android:name="com.example.colors.ui.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>